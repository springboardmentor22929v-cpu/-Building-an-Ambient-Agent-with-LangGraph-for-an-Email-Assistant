# Ambient Email Agent

AI-powered email management system using LangGraph.

## Setup
1. Copy `.env.example` to `.env`
2. Add your API keys
3. Run `pip install -r requirements.txt`
4. Place your `credentials.json` from Google Cloud Console
5. Run `python main.py`